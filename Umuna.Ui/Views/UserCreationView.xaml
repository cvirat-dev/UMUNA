<UserControl x:Class="Umuna.Ui.Views.UserCreationView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:attached="clr-namespace:Umuna.Ui.Controls.Attached"
	mc:Ignorable="d"
	d:DesignHeight="400" d:DesignWidth="600">
	
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
	 
		<TextBlock Text="Create User" FontSize="20" FontWeight="Bold"/>
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
			<TextBlock Text="Username:" Width="100" VerticalAlignment="Center"/>
			<TextBox Width="200" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
		</StackPanel>
		<StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
			<TextBlock Text="Email:" Width="100" VerticalAlignment="Center"/>
			<TextBox Width="200" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
		</StackPanel>
		<StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
			<TextBlock Text="Password:" Width="100" VerticalAlignment="Center"/>
            <PasswordBox Width="200" x:Name="PasswordBox" 
                         attached:PasswordBoxHelper.BindPassword="True"
                         attached:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay}"/>
		</StackPanel>
			<Button Grid.Row="4" Content="Create" Width="120" Margin="0,10,0,0"
				Command="{Binding CreateUserCommand}"/>
			<StackPanel Grid.Row="5" Margin="0,10,0,0">
			<TextBlock Foreground="Red" Text="{Binding ErrorMessage}"/>
			<TextBlock Foreground="Green" Text="{Binding SuccessMessage}"/>
		</StackPanel>
		
	</Grid>
	
</UserControl>