<UserControl x:Class="Umuna.Ui.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="450">
    <Grid Background="{StaticResource Brush.Background}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER AREA (styling aligned with MainView's header) -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="UMUNA Login" FontWeight="Bold"
                       VerticalAlignment="Center"/>
            <Button Content="Create Account" Margin="20,0,0,0" Width="140" Height="28"
                    Command="{Binding DataContext.ShowUserCreationCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>

        <!-- CONTENT -->
        <Border Grid.Row="1"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Username"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                         Width="240"/>

                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Password"/>
                <PasswordBox x:Name="PwdBox"
                             Grid.Row="1" Grid.Column="1"
                             Width="240"/>

                <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                           Foreground="OrangeRed"
                           Text="{Binding ErrorMessage}"
                           TextWrapping="Wrap" />

                <StackPanel Grid.Row="3" Grid.ColumnSpan="2"
                            Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="Login"
                            Width="100" Height="28"
                            Command="{Binding LoginCommand}"
                            CommandParameter="{Binding ElementName=PwdBox}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
