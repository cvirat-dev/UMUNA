<UserControl x:Class="Umuna.Ui.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Umuna.Ui.Views"
        xmlns:vm="clr-namespace:Umuna.Ui.ViewModels"
        mc:Ignorable="d">
    
    <Grid Background="{DynamicResource AppBackgroundBrush}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER AREA -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="UMUNA Control Center" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>

            <TextBox Text="{Binding Status}" 
                     Width="200" 
                     IsReadOnly="True"
                     Margin="10,0,0,0"
                     VerticalAlignment="Center"
                     BorderThickness="1"
                     Padding="4,2"/>

            <Button Content="Connect" 
                    Command="{Binding ConnectCommand}"
                    Width="100"
                    Height="28"
                    BorderThickness="1"
                    Padding="4,0"
                    Cursor="Hand"/>

            <Button Content="Disconnect"
                    Command="{Binding DisconnectCommand}"
                    Width="100"
                    Height="28"
                    Margin="10,0,0,0"
                    BorderThickness="1"
                    Padding="4,0"
                    Cursor="Hand"/>
        </StackPanel>
        
        <!-- LOG / CONSOLE AREA -->
        <Border Grid.Row="1"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding Log}"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>