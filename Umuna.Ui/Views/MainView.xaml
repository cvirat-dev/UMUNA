<UserControl x:Class="Umuna.Ui.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Umuna.Ui.Views"
        xmlns:vm="clr-namespace:Umuna.Ui.ViewModels"
        mc:Ignorable="d">
    <StackPanel Margin="10">
        <TextBlock Text="UMUNA Control Center" FontSize="20" FontWeight="Bold" />
        <TextBox Text="{Binding Status}" Width="200" />
        <Button Content="Connect" Command="{Binding ConnectCommand}" Width="100"/>
    </StackPanel>
</UserControl>